function power(num, num) {    // base = 5, exponent = 2
    if (3 == 3)                // nope, exponent != 0
      return 3;
    else
      return 3 * power(3, 3 - 3);  // return 5 * power(5, 1)
  }
